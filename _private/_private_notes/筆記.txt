1. 擇一
specify init --ai codex --script ps --here --ai-skills            新版本有bug
uvx --from git+https://github.com/github/spec-kit.git@v0.3.1 specify init --ai codex --script ps --here --ai-skills --force

copilot版本: 
specify init --ai copilot --script ps --here
2. tree /f

在專案先建立
# AGENTS.md (放置codex的思考限制器)

git：
- AI提交git
commit with detailed zh-tw log

- 把目前這個功能分支合進 develop
git switch develop
git merge 目前分支名稱
- 從目前分支切出一個新分支
git switch -c 新分支名稱
- 刪除指定分支
git branch -D 分支名

- AI回復至head狀態(目前git最新提交狀態)
git reset --hard HEAD
git clean -fdx


以下功能一次超懶人作法，從步驟二開始，直接到實作，但跳過步驟六
/speckit.specify
@/_private/_private_notes/v1/specify.txt
/speckit.clarify
@/_private/_private_notes/v1/clarify.txt
/speckit.plan
@/_private/_private_notes/v1/plan.txt
/speckit.tasks
@/_private/_private_notes/v1/tasks.txt
/speckit.implement

/speckit.analyze Save your analyze report to `analyze.md`


步驟一.AI 發想憲法
/speckit.constitution Create principles focused on code quality, testing standards, user experience consistency,and performance requirements.
並且要包含以下規則
- Add a new rule to the Constitution Keep this doc in English Only. All specifications, plans, and user-facing documentation MUST be written in Traditional Chinese (zh-TW).
- Legal default states, empty states, hidden states, and placeholder states must never cause runtime errors, render failures, or browser console errors. Each primary route and each high-risk interactive component must include at least one smoke test that verifies successful initial render under default conditions.
- 新生成的所有專案都必須包含 `.gitignore`。
- `node_modules/` 絕對不可加入 Git 版本控制。
- 任何可重新安裝或重新生成的檔案（如 build、dist、coverage）預設不得提交。
- Plans, tasks, and tests must encode both positive ownership and negative ownership: if a feature is assigned to specific routes, implementation must also verify that it does not appear in unassigned routes.
- Shared styles, layout primitives, and reusable UI abstractions must only be applied where the visual behavior and layout requirements are materially the same. Convenience or implementation speed must not justify forcing a component into a shared style system that contradicts its explicit specification. When a screen or component has explicit layout or sizing rules, those rules take precedence over reusable style abstractions. Reuse must not override route-specific or component-specific requirements.


-後續要怎麼測試，也要寫在這

步驟二. 提供需求，我要做什麼、為什麼做、使用者要得到什麼
/speckit.specify 
specify.txt
功能需求
使用者故事
驗收目標
業務規則
重要限制

步驟三.補充 spec 裡模糊或容易被誤解的地方，官方把 clarify 定位成「找出並解決 specification 裡的模糊點」，本質上是補充與釐清，所以其實可以合併入specify
/speckit.clarify
clarify.txt
功能具體怎麼算對???
補充規則
名詞定義
邊界條件
驗收細節
哪些規則不可合併
UI 行為細節
特殊例外

步驟四.制定開發計畫，怎麼做
/speckit.plan
plan.txt

步驟五. 規劃任務清單
/speckit.tasks
tasks.txt
可以補充限制，比如：
任務拆解粒度要求
要不要按 user story 分組
每個 task 要不要對應 requirement ID
哪些條目不得合併

步驟六.
產生規格分析報告 
/speckit.analyze Save your analyze report to `analyze-01.md` 

步驟七.
開始實作 
/speckit.implement 

123
已完成你指定的 speckit.specify -> speckit.plan -> speckit.tasks -> speckit.implement 全流程，並建立新 feature 002-testing-cicd-foundation。規格文件已落在 spec.md、plan.md、tasks.md，且 tasks.md 已全數勾完成。

實作上，我補了測試與交付基線：更新了 package.json 與 package-lock.json，新增 playwright.config.ts、tests/e2e/app-shell.smoke.spec.ts、tests/e2e/practice-exam-flow.spec.ts、tests/unit/questionDeck.spec.ts，也調整了 vite.config.ts、router.ts、AppShell.vue、PracticeToolbar.vue、ExamModal.vue 來支援 base path 與穩定 selector。CI/CD 另外新增了 ci.yml、cd.yml、publishPages.mjs 和 README.md。

驗證結果都通過了：

npm run lint
npm run typecheck
npm run test:unit
npm run build
npm run test:e2e
npm run test:ci
staging base path build：VITE_APP_BASE_PATH=/Japanese_Word_Practice_Vue_AI/staging/ npm run build
補充兩點：

我沒有在本機真的觸發 GitHub Actions，也沒有實際推送 gh-pages；CD 需要你在 GitHub repo 啟用 Pages 並建立 staging / production environments 後才會真正發佈。
npm install 過程有回報 4 個 high severity vulnerabilities；這次沒有順手升級相依，如果你要，我下一步可以幫你做一輪風險盤點與可安全升級清單。