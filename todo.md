# 專案補強 TODO

## 掃描摘要

- 專案主體目前分成 `practice`、`grammar`、`n5Grammar`、`vocabulary` 四個模組，對應的 component / unit / e2e 測試都有基本覆蓋。
- `specs/` 內已有完整 spec-kit 軌跡，`016-n5-change-invitation` 是目前和 `/n5-grammar` 最直接相關的規格包。
- `/n5-grammar` 目前已有獨立資料層、renderer 與 route view，教學內容集中在 `src/modules/n5Grammar/data/grammarNotes.ts`。
- 目前工作樹的主要變更集中在 `n5Grammar` 相關資料與測試，代表現階段最值得繼續收斂的是 N5 文法內容品質，而不是架構重做。

## 高優先補強

- [ ] `state-change-naru`：把 `～くなります / ～になります` 補成和 `state-change-suru` 同一層級的「理解式拆解」。
  - `い形容詞`：補成「先去 `い` 變 `く`，再接 `なります`；可理解成把狀態轉成後續可接變化的形」。
  - `な形容詞`：補成「`に` 標示變化後的狀態，不只是死背接法」。
  - `名詞`：補成「`に` 標示變化結果／到達的身分或時間點」。
- [ ] `i-adjective-basics`：把 `くないです` 補成更好記的結構說明。
  - 可補「不是直接在後面加否定，而是先把 `い` 改成 `く`，再接 `ないです`」。
  - `いい / よい` 可再補一句「活用時一律走 `よい` 系列」。
- [ ] `i-adjective-past`：把過去肯定與過去否定補成有步驟感的說明。
  - `～かったです`
  - `～くなかったです`
  - 可補成「先變否定，再變過去」的理解路徑。
- [ ] `nominal-past`：補上 `です → でした`、`じゃありません → じゃありませんでした` 的對照式理解。
- [ ] `verb-past`：補上 `ます → ました`、`ません → ませんでした` 的明確拆解，和邀約段落的「從ます形出發」互相呼應。

## 中優先補強

- [ ] `continuous-state`：補一句「`て形 + います / いました` 不只表示進行，也常表示狀態持續」。
- [ ] `invitation-core-difference`：把目前已有的 `ます形去掉ます` 規則整理得更工整，讓 `ませんか` / `ましょう` 的來源形式更對稱。
- [ ] `mashou-plain-volitional`：補上「`ましょう` 是敬體提議，`帰ろう` 是普通體意向形」的系統差異，避免看起來像單純換字尾。
- [ ] `masu-verb-basics`：補一句「`ます` 不是現在進行式，而是敬體基本形，可表習慣、預定與一般敘述」。
- [ ] `particle-ni`：在時間點用法外，可再補成「把動作掛到明確時間錨點上」的白話理解。
- [ ] `particle-he` vs `particle-ni`：再補一層「`へ` 偏方向、`に` 偏到達點」的記憶式對照。

## 低優先補強

- [ ] `particle-wa`：局部補強「A は B」可理解成「關於 A，說明 B」的拆解型提示，讓初學者更不容易把 `は` 和主語畫上等號。
- [ ] `particle-ga`：挑 1 到 2 個代表例句補成「句中哪一塊是正在被描述的狀態主體」。
- [ ] `particle-wo`：在既有受詞說明上，再補一句「動作直接作用到哪裡」的白話版本。
- [ ] `particle-de`：目前偏交通工具與手段，可考慮補成「用什麼方式做」的總括語感。

## 一致性補強

- [ ] 補齊目前仍為空字串的 `summary` / `description`，避免資料層和既有單元測試期待不一致。
- [ ] 統一 `note` 文案格式。
  - 例如助詞解釋、詞性拆解、變化路徑，盡量維持相同句型與標點風格。
- [ ] 建立一條簡單規則：
  - `summary` 放主理解
  - `details` 放規則拆解
  - `note` 放單句例句中的局部語法提醒
- [ ] 檢查 `origin: "source"` 與 `origin: "supplemental"` 的使用標準，避免新增解釋時標記風格越來越混亂。

## 測試與維護

- [ ] 每補完一組核心文法，就同步加一條對應 unit test，鎖住代表句與關鍵說明。
- [ ] 若未來持續增加「理解式補充」，可考慮新增一條資料測試，檢查核心 topic 至少有非空 `summary`，避免內容只剩例句與 note。
- [ ] `viewPreferences.ts` 目前已抽離成獨立設定，之後若預設展開 section 變多，可補一條簡單測試鎖住設定行為。

## 建議實作順序

1. 先補 `state-change-naru`
2. 再補 `i-adjective-basics` 與 `i-adjective-past`
3. 接著補 `nominal-past`、`verb-past`
4. 最後再收 `continuous-state`、`invitation` 與助詞區塊

## grammarNotes.ts 修改前

### i-adjective-basics

```ts
summary:
  "い形容詞放在句尾時直接接「です」；做否定時要先把詞尾「い」改成「く」，再接「ないです」。",
details: [
  "「いいです」與「よいです」在現在肯定都可以使用，但做變化時統一以「よい」系列活用。",
  "因此現在否定要說「よくないです」，不是「いいくないです」。",
],
```

### i-adjective-past

```ts
summary:
  "い形容詞要變成過去肯定時，把詞尾「い」改成「かったです」；要變成過去否定時，則改成「くなかったです」。",
details: [
  "「いいです」的過去與否定仍然走「よい」系列，所以要說「よかったです / よくなかったです」。",
  "這一類句子也常與「から」連用，表達造成後句動作的原因。",
],
```

### state-change-naru

```ts
description: "述狀態變化、變成某種樣子或結果時。(非人為刻意影響)",
```

#### naru-i-adjective

```ts
summary: "",
details: [
  "い形容詞先把詞尾「い」改成「く」，再接「なります」",
  "如果要說已經變成那樣，則用過去形「くなりました」",
],
```

#### naru-na-adjective

```ts
summary: "",
details: [
  "な形容詞直接接「になります」",
  "如果要表達已經變成那樣，就改成過去形「になりました」",
],
```

#### naru-noun

```ts
summary:
  "用來表示變成某種身分、進入某個時間點，或變成某種結果狀態；整體上也帶有變成那樣的感覺。",
details: [
  "名詞直接接「になります」",
  "如果要表達已經變成那樣，或已經到了那個時間點，就改成過去形「になりました」",
],
```
